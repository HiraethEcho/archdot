#!/bin/bash
case "$1" in
  1) # general
    case "$2" in 
      1) rofi_allapps ;;
      2) kitty -e btop ;;
      3) rofi_powermenu ;;
    esac
    ;;
  2) # win app
    case "$2" in 
      1) microsoft-edge-stable ;;
      2) onedrivegui ;;
      3) wechat-universal ;;
    esac
    ;;
  3) # files
    case "$2" in 
      1) thunar ;;
      2) obsidian ;;
      3) zotero ;;
    esac
    ;;
  4) # clouds & sync
    case "$2" in 
      # 1) alist ;;
      2) unison-gui ;;
      3) gitg ;;
      4) ;; #upload
      5) ;; #download
    esac
    ;;
esac
